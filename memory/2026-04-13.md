# 2026-04-13

- Se logró automatizar Firmaya usando Playwright con sesión persistente ya logueada, sin pedir credenciales por chat.
- Se identificó la ruta real de descarga PDF por documento: `/groups/download_pdf_doc?id=<docId>`.
- Se creó y validó `firmaya_download_all.js` para recorrer `my_signed_docs`, paginar, mapear documentos y descargarlos.
- Primera pasada: 435 documentos detectados, pero varios se pisaban por nombres duplicados.
- Segunda pasada corregida con nombres únicos por `docId` cuando hay colisión, guardando en `state/firmaya-by-investor-v2`.
- Resultado final de la segunda pasada: 435 documentos detectados, 433 PDFs descargados, 2 pendientes que aparentan ser documentos inválidos (`error`, `NO VALIDO`) y no disparan descarga.
- Se dejó evidencia de fallos en `state/firmaya-by-investor-v2/_failures.json` y resumen en `_summary.json`.
- Se instaló `pypdf` en `.venv-audio` para leer texto de los PDFs descargados.
- Se confirmó extracción legible desde contratos reales: capital, tasa, plazo, tabla de pagos, cuenta bancaria, CVE y datos del inversionista.
- Clasificación inicial del universo descargado: 357 contratos de mutuo, 30 fin de contrato de mutuo, 16 mandatos/poderes, 12 arriendos y 18 otros.
- Se generó dataset inicial estructurado en `state/firmaya-by-investor-v2/_mutuo_dataset.json` y `_mutuo_dataset.csv` con 387 filas (357 mutuos + 30 fin de contrato).
- Regla de negocio crítica dada por Andrés: un `fin de contrato` suele significar renovación anticipada mientras el mutuo seguía vigente.
- Otra regla crítica: la ausencia de `fin de contrato` NO implica vigencia actual, porque muchas renovaciones ocurren simplemente al vencer el plazo natural y se crea un nuevo mutuo sin documento de cierre.
- La nomenclatura secuencial importa: `Contrato X`, luego `Contrato X 2`, `Contrato X 3`, etc. El primero normalmente no lleva `1`.
- La futura base debe modelar `familias` o `líneas de renovación` por inversionista, no tratar cada PDF como obligación aislada.
- Nueva regla crítica dada por Andrés: muchas renovaciones mantienen exactamente el mismo capital y reinician el conteo de meses en un contrato nuevo (`contrato 2`, `3`, etc.).
- Otras renovaciones aumentan el capital, por lo que la base debe distinguir entre capital rolado y capital incremental.
- Otra regla crítica dada por Andrés: `mutuo` y `mutuo express` son líneas distintas. El mismo inversionista puede tener `Mutuo 2` y `Express 2`, y esos números no deben mezclarse.
- Próximo input importante acordado: un Google Sheets con contratos pagados, que se usará como tercera capa de evidencia después de Firmaya y la carpeta auxiliar de mutuos.
- Andrés aclaró luego que el Google Sheet está aprox. 95% correcto y tiene alta confianza operativa; errores conocidos: algunos nombres mal escritos y algunos montos 0 en casos renovados no limpiados del flujo.
- Otra regla crítica dada por Andrés: en su uso manual suele abreviar nombres o usar alias cortos (`Carranza`, `Vivianne`, etc.), por lo que el cruce debe soportar alias humanos y no exigir nombres completos exactos.
- Se creó una base maestra consolidada `master_cashflow_base.csv/json`.
- Con revisión manual de Andrés se cerraron los últimos casos ambiguos y la base quedó con `0 SIN_CLASIFICAR`.
- Estado final de la base: 151 `RENOVADO_CON_PAGOS`, 61 `PAGADO`, 34 `LIQUIDADO`, 15 `RENOVADO`, 6 `RENOVADO_VIGENTE`, 1 `VIGENTE`, 1 `DESCARTAR`, 4 `POST_CORTE_SHEET`.
- La base maestra fue publicada en el Google Sheet compartido, en pestañas `BASE MAESTRA` y `RESUMEN BASE`, y se limpiaron pestañas vacías de prueba.
# 2026-04-13

- Se logró automatizar Firmaya usando Playwright con sesión persistente ya logueada, sin pedir credenciales por chat.
- Se identificó la ruta real de descarga PDF por documento: `/groups/download_pdf_doc?id=<docId>`.
- Se creó y validó `firmaya_download_all.js` para recorrer `my_signed_docs`, paginar, mapear documentos y descargarlos.
- Primera pasada: 435 documentos detectados, pero varios se pisaban por nombres duplicados.
- Segunda pasada corregida con nombres únicos por `docId` cuando hay colisión, guardando en `state/firmaya-by-investor-v2`.
- Resultado final de la segunda pasada: 435 documentos detectados, 433 PDFs descargados, 2 pendientes que aparentan ser documentos inválidos (`error`, `NO VALIDO`) y no disparan descarga.
- Se dejó evidencia de fallos en `state/firmaya-by-investor-v2/_failures.json` y resumen en `_summary.json`.
- Se instaló `pypdf` en `.venv-audio` para leer texto de los PDFs descargados.
- Se confirmó extracción legible desde contratos reales: capital, tasa, plazo, tabla de pagos, cuenta bancaria, CVE y datos del inversionista.
- Clasificación inicial del universo descargado: 357 contratos de mutuo, 30 fin de contrato de mutuo, 16 mandatos/poderes, 12 arriendos y 18 otros.
- Se generó dataset inicial estructurado en `state/firmaya-by-investor-v2/_mutuo_dataset.json` y `_mutuo_dataset.csv` con 387 filas (357 mutuos + 30 fin de contrato).
- Regla de negocio crítica dada por Andrés: un `fin de contrato` suele significar renovación anticipada mientras el mutuo seguía vigente.
- Otra regla crítica: la ausencia de `fin de contrato` NO implica vigencia actual, porque muchas renovaciones ocurren simplemente al vencer el plazo natural y se crea un nuevo mutuo sin documento de cierre.
- La nomenclatura secuencial importa: `Contrato X`, luego `Contrato X 2`, `Contrato X 3`, etc. El primero normalmente no lleva `1`.
- La futura base debe modelar `familias` o `líneas de renovación` por inversionista, no tratar cada PDF como obligación aislada.
- Nueva regla crítica dada por Andrés: muchas renovaciones mantienen exactamente el mismo capital y reinician el conteo de meses en un contrato nuevo (`contrato 2`, `3`, etc.).
- Otras renovaciones aumentan el capital, por lo que la base debe distinguir entre capital rolado y capital incremental.
- Otra regla crítica dada por Andrés: `mutuo` y `mutuo express` son líneas distintas. El mismo inversionista puede tener `Mutuo 2` y `Express 2`, y esos números no deben mezclarse.
- Próximo input importante acordado: un Google Sheets con contratos pagados, que se usará como tercera capa de evidencia después de Firmaya y la carpeta auxiliar de mutuos.
- Andrés aclaró luego que el Google Sheet está aprox. 95% correcto y tiene alta confianza operativa; errores conocidos: algunos nombres mal escritos y algunos montos 0 en casos renovados no limpiados del flujo.
- Otra regla crítica dada por Andrés: en su uso manual suele abreviar nombres o usar alias cortos (`Carranza`, `Vivianne`, etc.), por lo que el cruce debe soportar alias humanos y no exigir nombres completos exactos.
- Se creó una base maestra consolidada `master_cashflow_base.csv/json`.
- Con revisión manual de Andrés se cerraron los últimos casos ambiguos y la base quedó con `0 SIN_CLASIFICAR`.
- Estado final de la base: 151 `RENOVADO_CON_PAGOS`, 61 `PAGADO`, 34 `LIQUIDADO`, 15 `RENOVADO`, 6 `RENOVADO_VIGENTE`, 1 `VIGENTE`, 1 `DESCARTAR`, 4 `POST_CORTE_SHEET`.
- La base maestra fue publicada en el Google Sheet compartido, en pestañas `BASE MAESTRA` y `RESUMEN BASE`, y se limpiaron pestañas vacías de prueba.
- Regla nueva crítica: el Google Sheet es la “tabla de la verdad” operativa, pero solo hasta su último corte de actualización. Andrés dijo que quedó actualizado aproximadamente hasta mediados de marzo de 2026; contratos posteriores pueden no aparecer y no deben interpretarse automáticamente como error o impago.
- Se incorporó el estado `POST_CORTE_SHEET` para contratos nuevos posteriores al corte del Sheet que aún no aparecen cargados.
- Confirmaciones manuales importantes de Andrés para la clasificación final: `Santiago La Corte` es el mismo inversionista que `Amelia Salas`; varios contratos `express` de `Vivianne Castaing` siguen renovados y vigentes; `Carlos Núñez express 06` sigue vigente; `Amara Arroyo express` y `Lucas Mardones express 03` migraron/renovaron a una línea más larga; `Sofía Jimeno` y `Amelia Salas` ya salieron/liquidadas.
- Andrés explicó que además de la base maestra necesita una vista tipo “Hoja 1” ordenada por fecha para funcionar como calendario operativo de pagos y luego como insumo del flujo de caja.
- Se reconstruyó un calendario de pagos desde los contratos (`payment_calendar_like_sheet.csv/json`) usando fechas de pago extraídas desde PDFs, deduplicando versiones repetidas.
- Se generó una vista simple tipo hoja operativa (`payment_calendar_simple_view.csv`) con columnas `NOMBRE`, `FECHA`, `MONTO`, `STATUS` más columnas auxiliares.
- El calendario reconstruido quedó con 1499 filas, desde `2024-05-10` hasta `2028-03-21`; la hoja original tenía 1540 filas y 1447 filas del calendario caen en fechas también presentes en la hoja original tras normalizar fechas.
- Se subieron al Google Sheet dos pestañas nuevas: `CALENDARIO SIMPLE` y `CALENDARIO PAGOS V1`.
- Andrés corrigió una regla conceptual crítica para el calendario: el status de una fila de pago de interés NO debe heredar el status del contrato. `RENOVADO` o `LIQUIDADO` describen el capital al vencimiento, no la cuota mensual.
- Regla práctica nueva: en el calendario, la fila de interés mensual debe tratarse aparte del movimiento de capital. En la fecha de vencimiento pueden coexistir dos filas: una de interés (`PAGADO` o `POR PAGAR`) y otra de capital (`RENOVADO`, `LIQUIDADO`, `VIGENTE`, etc.).
- Se construyó `payment_calendar_unified_v2.csv/json`, que parte desde 2023, preserva las filas históricas de la hoja original y agrega filas generadas desde contratos para completar lo faltante y proyectar hacia adelante.
- La pestaña `CALENDARIO SIMPLE V2` fue subida al Google Sheet como nueva versión corregida del calendario.
- Andrés pidió explícitamente que la corrección de status se haga “desde Hoja 1”, es decir, tomando la hoja original como fuente dominante para histórico antes de inferir nada desde contratos.
- Se construyó `payment_calendar_unified_v3.csv/json`, donde 1540 filas conservan status directo de Hoja 1 y 798 filas generadas logran heredar status por match con Hoja 1 (base + fecha + monto) antes de aplicar reglas.
- En la V3 quedaron aún 489 filas con status por regla de fecha y 157 filas con status de capital contractual, que son el siguiente subconjunto a auditar.
- Se subió la pestaña `CALENDARIO SIMPLE V3` al Google Sheet compartido.
- Andrés pidió agregar una columna de familia al calendario para poder agrupar y responder preguntas por familia/inversionista (capital invertido, intereses pagados acumulados, etc.).
- Se construyó `payment_calendar_unified_v4.csv` agregando columnas `FAMILIA` y `FAMILIA_BASE_ID` al calendario unificado.
- La V4 resolvió familia en 2905 de 2984 filas, dejando 79 aún sin familia.
- Se subió la pestaña `CALENDARIO SIMPLE V4` al Google Sheet compartido.
